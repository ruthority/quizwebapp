{
  "name": "quiz web app",
  "version": "1.0.0",
  "description": "Quiz web application using an api",
  "main": "index.html",
  "scripts": {
    "start": "http-server -p $PORT"
  },
  "dependencies": {
    "http-server": "^0.12.3"
  }
}
